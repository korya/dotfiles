# vim: set filetype=sh :

if [[ -z "${BASH_VERSION}" ]]; then
  return
fi

complete -o filenames -F _longopt grepx grepm grepc
complete -c rwhich

### _ssh_umount()
### {
###     local cur
### 
###     COMPREPLY=()
###     cur=${COMP_WORDS[COMP_CWORD]}
### 
###     COMPREPLY=( $(compgen -W "$(ls $HOME/mnt)" -- $cur) )
### }
### complete -o dirnames -F _ssh_umount ssh_umount
